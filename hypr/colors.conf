general {
    # Active window border color (matching --color13: #69C3FF)
    col.active_border = rgb(69C3FF)

    # Inactive window border color (matching --color4: #a1adb7)
    col.inactive_border = rgb(77AED7)
}

misc {
    # Background color (matching --background: #1c2433 with full opacity)
    background_color = rgba(1C2433FF)
}

# Border color for pinned windows
windowrulev2 = bordercolor rgba(69C3FFAA) rgba(A1ADB799),pinned:1
